makeDummyTodo (index) return newTodo
makeDummyProject (noTodos, index) return newProject
makeDummyProjectList (noProjects) return newProjectList

(dummyData = makeDummyProjectList)

updateTodo (projectNo, modifiedData) // modifies dummyData directly
makeTodo (projectNo, inputData) // modifies dummyData directly
makeProject (newProjectTitle) // modifies dummyData directly

projectClickHandler (e) // displayTodos(selectedProject)

displayProjects (projectList) // n * projectContainer.appendChild()

displayTodos (projectNo) // n * newTodoContainer.appendChild()

todoClickHandler (e) // displayData(selectedTodo)

modifyTodo (currentProject, todoData) // create modal, onclick=updateTodo(currentProject.index, modifiedData)

displayData (todoNo) // dataContainer.appendChild()

createNewProject () // create modal, onclick=makeProject(newProjectTitle)

createNewTodo (projectNo) // create modal, onclick=makeTodo(projectNo, newTodo)

projectList[ <project0>{name: name, index: index, data: {} }]
project-data: [ <todo0>{name: name, desc: desc, duedate: duedate, notes: notes, index: index} ]

eg projectList[project-index].data[todo-index].notes